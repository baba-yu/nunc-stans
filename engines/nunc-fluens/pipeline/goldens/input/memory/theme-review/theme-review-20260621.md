# Theme review — week ending 2026-06-21

Mode: routine Sunday rotation (5_weekly_theme_review). Diagnostic + advisory pass over the current schema. Inputs: `docs/data/graph-{tech,business,mix}.json` (post-update_pages snapshot, generated 2026-06-21T14:43Z), `theme_candidates` table (read-only, 42 pending rows), `app/src/schema.sql`.

## Diagnostic summary

Total themes: tech 10, business 8, mix 18 (unchanged). Total predictions: tech 170, business 182, mix 197 unique. Pending `theme_candidates`: 42 rows (15 business-scope, 27 tech-scope), all `candidate_reason='no_keyword_match'`, none carrying a `nearest_theme_id`. Max within-scope hit count = 1 across all 42 labels (each label is a distinct one-off prediction string). Ninth consecutive week the `≥ 3 distinct-day` promotion bar cannot fire on raw labels (5/10, 5/17, 5/24, 5/31, 6/07, 6/14, this week).

**The headline this week: last week's organic relief on `business.ai_revenue_disclosure` REVERSED, and recommendation 1 (sharpen its description) STILL never landed.** The most recent schema commit remains `e5e0a79` (applied 5/26, from the 5/24 review); neither the 5/31, the 6/07, the 6/14, nor any intervening `rewrite-description` block reached `app/src/schema.sql`. The theme's description at `app/src/schema.sql:1234` still carries every broad anchor token the sharpening was meant to drop ("Big-3", "foundation labs (OpenAI, Anthropic, xAI)", "Microsoft Azure", "Alphabet", "AMD", bare company names) — the `Big-3` / `foundation labs (OpenAI` anchors are confirmed present in the current schema by direct grep (4 hits). Four straight weeks unchanged.

What *changed* this week is the opposite of last week, and it confirms the relief was matcher noise. Over the past week new predictions landed and the IDF tournament re-ran against the **unchanged** schema, and the result moved straight back the way 6/14 warned it could: `business.ai_revenue_disclosure` primary kids rose **90 → 102** (child_ids 141 → 160), and `business.capital-supply-chain` category dominance ticked back up **70.2 % → 71.4 %**, erasing the single-week dip and re-confirming the five-week climb pattern (52.2 % → 68.9 % → 74.0 % → 76.0 % → 70.2 % → 71.4 %). `business.compute_capex_strategy` continued to grow organically (67 / 23 → **82 / 28**), so the capex-strategy revival is durable on its own merits — but it did not pull load off `business.ai_revenue_disclosure` this week; the disclosure theme simply re-absorbed the new business-scope predictions that had no closer match. This is exactly the "reversible on a quiet match week" failure mode flagged on 6/14. At 102 primary the theme is by a wide margin the largest in any scope (the next-largest primary core anywhere is tech.model_supply_chain at 57), its description is still the broadest of the 8 business themes, and the relief that looked promising last Sunday is gone. Recommendation 1 below re-emits the same vetted sharpening, unchanged, so the improvement is locked into the schema rather than left to weekly matcher noise.

### Tech scope — theme attachments + category density

| Theme | Category | child_ids | primary |
|---|---|---|---|
| Model Supply Chain (`tech.model_supply_chain`) | tech.security | 117 | 57 |
| AI Macro & Capital Markets (`tech.ai_macro_capital_markets`) | tech.infrastructure | 64 | 34 |
| Agent Runtime Security (`tech.agent_runtime_security`) | tech.security | 51 | 13 |
| Agent Registry Architecture (`tech.agent_registry_architecture`) | tech.standards | 32 | 9 |
| Frontier Model Regulatory Board (`tech.frontier_model_regulatory_board`) | tech.standards | 31 | 16 |
| Agent Control Plane (`tech.agent_control_plane`) | tech.agents | 26 | 13 |
| Local Inference Runtime (`tech.local_inference_runtime`) | tech.inference-runtime | 19 | 11 |
| 1-bit / Edge LLM (`tech.one_bit_edge_llm`) | tech.models | 17 | 5 |
| AI Chip Architecture (`tech.ai_chip_architecture`) | tech.infrastructure | 12 | 4 |
| Physical AI / Robotics (`tech.physical_ai_robotics`) | tech.infrastructure | 11 | 8 |

Tech category density (primary attach via prediction `category_id`, 170 distinct predictions): tech.security 70 / 170 = 41.2 %, tech.infrastructure 46 / 170 = 27.1 %, tech.standards 25 / 170 = 14.7 %, tech.agents 13 / 170 = 7.6 %, tech.inference-runtime 11 / 170 = 6.5 %, tech.models 5 / 170 = 2.9 %. No category over 50 %. tech.security held its top-tech position from last week and edged up (model-supply-chain grew to 117 / 57), but both it and tech.infrastructure sit comfortably below threshold and the FMRB carve-out continues to hold tech.standards balanced. Tech taxonomy remains healthy.

### Business scope — theme attachments + category density

| Theme | Category | child_ids | primary |
|---|---|---|---|
| AI-Revenue Disclosure Rewrite (`business.ai_revenue_disclosure`) | business.capital-supply-chain | 160 | 102 |
| Inference Server Supply Chain (`business.inference_server_supply_chain`) | business.regulation-compliance | 91 | 24 |
| Compute Capex Strategy (`business.compute_capex_strategy`) | business.capital-supply-chain | 82 | 28 |
| Developer Toolchain Platformization (`business.developer_platformization`) | business.enterprise-adoption | 46 | 10 |
| Hyperscaler × Frontier Lab Alliance (`business.hyperscaler_frontier_lab_alliance`) | business.market-structure | 42 | 3 |
| Open Weight vs Proprietary AI (`business.open_weight_vs_proprietary`) | business.competition | 38 | 12 |
| Cloud vs Local AI Distribution (`business.cloud_vs_local_distribution`) | business.distribution | 26 | 1 |
| AI Security Compliance Market (`business.ai_security_compliance_market`) | business.regulation-compliance | 24 | 2 |

Business category density (primary attach, 182 distinct predictions): **business.capital-supply-chain 130 / 182 = 71.4 %** (back up from 70.2 %), business.regulation-compliance 26 / 182 = 14.3 %, business.competition 12 / 182 = 6.6 %, business.enterprise-adoption 10 / 182 = 5.5 %, business.market-structure 3 / 182 = 1.6 %, business.distribution 1 / 182 = 0.5 %. Single-category dominance on business climbed back this week and remains the project's only category over the 50 % flag line. The 6/14 hope that the two capital-supply-chain themes would keep splitting the load did not hold: `business.compute_capex_strategy` grew its own primary core (23 → 28) but `business.ai_revenue_disclosure` grew faster (90 → 102), so the category re-concentrated rather than balanced. The structural fix (a new category to pull `business.ai_revenue_disclosure` out of `business.capital-supply-chain`) remains the durable answer; see recommendation 3.

### Mix scope

Rolls up tech + business; same theme + category shape as above, with primary re-tournamented across the combined 18-theme pool. Total unique predictions = 197. In the mix pool, several business themes shed nearly all their primary kids to better-matching tech themes — `business.ai_revenue_disclosure` drops to 15 primary (from 102 in business-only), `business.inference_server_supply_chain` and `business.ai_security_compliance_market` fall to 0 primary, `business.developer_platformization` / `business.open_weight_vs_proprietary` / `business.cloud_vs_local_distribution` to 1 each — confirming these clusters are tech-flavored predictions parked in the business pool only because no tech theme competes in business-only mode. Mix category density is well-distributed: top is tech.security at 35.5 %, no category over 50 %.

### Multi-attach pressure (secondary-attach path in `app/src/export.py`)

Distribution of theme-parents per prediction (counted via theme-node `child_ids` membership):

- Tech (170 predictions): 60 attach to exactly one theme, 48 to two, 35 to three, 20 to four, 4 to five, 2 to six, 1 to seven. 110 / 170 = 64.7 % multi-attach.
- Business (182 predictions): 46 to one, 46 to two, 35 to three, 25 to four, 17 to five, 10 to six, 3 to seven. 136 / 182 = 74.7 % multi-attach.
- Mix (197 predictions): 20 to one, 28 to two, 32 to three, 34 to four, 21 to five, 16 to six, 18 to seven, 10 to eight, 12 to nine, 3 to ten, 2 to eleven, 1 to fourteen. 177 / 197 = **89.8 % multi-attach**.

The same broad-context-prediction pattern flagged every week since 4/27 — tenth consecutive week. All three scopes ticked up slightly (tech 62.1 → 64.7 %, business 71.4 → 74.7 %, mix 88.6 → 89.8 %) as the prediction pool grew. This is an `export.py` secondary-attach-threshold property, not a schema fault; carried forward as an engineering item (recommendation 4).

## Empty / underused themes

No theme is empty (child/prediction count 0 or 1) on the primary `child_ids` measure. The two lowest-primary themes:

- **`business.cloud_vs_local_distribution` — 26 children, 1 primary.** Healthy child count (26, up from 19) keeps it visible on the dashboard via secondary attach, but its primary core is the thinnest of any theme and unchanged at 1 from 6/14. It carves a genuinely distinct sub-space (on-device vs hosted distribution shifts, cloud-overflow inversion, SMB local-first) that does not overlap any other business theme's keyword set, so it is not a merge candidate — it simply wins few IDF tournaments because most distribution-flavored predictions also carry stronger capital / compliance signal. Log-only watch; second consecutive week at 1 primary, flag for keyword expansion if it stays ≤ 1 next week.
- **`business.ai_security_compliance_market` — 24 children, 2 primary (0 primary in mix scope).** Unchanged posture from 6/14 (was 20 / 2). Healthy child count keeps it visible; its primary core stays thin because it competes against `business.inference_server_supply_chain` (same `business.regulation-compliance` category, 24 primary) for regulation/compliance predictions. Still not a merge candidate — the two themes carve genuinely different sub-spaces (compliance-market/spend vs supply-chain-CVE-governance) and the keyword sets are distinct. Log-only watch; flag if primary stays ≤ 2 next week.

The 6/14 close on `business.compute_capex_strategy` holds: it grew again this week to 82 children / 28 primary on its 5/24-sharpened description and is firmly a healthy, well-populated theme. No regression there.

## Overpopulated themes

Several themes exceed the ≥ 6-prediction threshold; all are coherent except the one persistent structural concern:

- **`business.ai_revenue_disclosure` — 160 children, 102 primary. PERSISTENT, STILL-UNRESOLVED PAIN POINT.** Up from 141 / 90 on 6/14, reversing last week's matcher-driven dip — the sharpening has still never landed (description unchanged for four weeks; see Diagnostic summary). The 5/31 audit found ~42 of the primary kids genuinely about AI-revenue disclosure (SEC concept release, audited monthly-revenue + WAU cadence, 10-Q footnote breakouts, AI-accelerator S-1 / IPO cohort, per-token-margin reporting) and the rest off-topic (KV-cache deployment knobs, MoE LoRA recipes, MCP RFCs, Linux-kernel CVE chains, CISA agentic-AI guides, OCC Model Risk Management, sandboxed-skill verification, edge-LLM recipes, physical-AI procurement). At 102 primary the theme is now ~60 above the genuine-disclosure core and back to its widest spread since 6/07. Root cause is unchanged and structural: this theme's keyword set is the broadest of the 8 business themes, so the IDF tournament defaults to it whenever a business-scope prediction has no closer match. The fix is the description sharpening in recommendation 1 — re-emitted unchanged because it never landed.
- `business.inference_server_supply_chain` (91 children, 24 primary, 67 secondary) — primary core coherent (inference-server CVE class, CISA AI-Infra KEV sub-catalog, signed-checkpoint loading, NIST non-human-identity profile, agent-skills threat sub-matrix). Multi-attach inflation is `export.py`-driven. Leave as-is.
- `tech.model_supply_chain` (117 children, 57 primary) — continued strong growth this week and still the largest tech theme by both measures. Primary core coherent around model signing / provenance / loader-verification (the loader-verification keyword set widened on 5/24 is matching well). The high child count is secondary-attach inflation, not drift. Leave as-is.
- `tech.ai_macro_capital_markets` (64 children, 34 primary) — primary core uniform around macro / capital-markets dynamics. Leave as-is.
- `business.compute_capex_strategy` (82 children, 28 primary) — continues to grow on its 5/24-sharpened description (commit `e5e0a79`); primary core coherent around multi-year compute-capex commitments, GW-scale buildouts, forward-supply 8-K cadence. Leave as-is.
- `tech.frontier_model_regulatory_board` (31 children, 16 primary) — primary core coherent (FMRB executive order, AISI cyber-eval reciprocity ladder, allied procurement mirror clauses, allied AI-safety-institute mutual recognition). No drift. Leave as-is.

## Theme candidates

No candidate label reaches the ≥ 3-hit raw-label bar (max within-scope hit count = 1). Manual topical clustering of the 42 pending rows surfaces the following groups, all already covered by existing themes:

- AI-Revenue / SEC-disclosure cluster — ~9 rows across both scopes (SEC AI-accelerator IPO Staff Legal Bulletin, SEC Corp Fin AI-accelerator S-1 staff guidance, SEC AI-compute-prebuy disclosure concept release, AI-accelerator S-1 per-counterparty risk-factor table, frontier-lab chip-supply warrant S-1 disclosure standard, Anthropic confidential S-1, AI-accelerator IPO cohort second >$10B deal, AGI-clause unwind to peer contracts, AI-accelerator XPU $20B/quarter run-rate). Covered by `business.ai_revenue_disclosure` (and `tech.ai_macro_capital_markets` on the tech side). These are exactly the genuine-disclosure predictions; once recommendation 1 sharpens the theme, they should match more cleanly rather than tie with off-topic kids.
- FMRB / allied cyber-eval reciprocity cluster — 4 rows (FMRB executive order ×3 wordings, OECD GENAI cyber-eval reciprocity attestation Recommendation, allied procurement mirror clause). Covered by the live `tech.frontier_model_regulatory_board`.
- Frontier-lab voluntary-governance / safety-framework convergence — 2 rows, both tech, both Q2 2027 ("Frontier-lab governance frameworks converge on shared quantitative-harm thresholds", "Frontier-lab safety frameworks align on catastrophic-risk disclosure"). These are voluntary lab-side framework convergence, conceptually distinct from the regulatory-board / procurement-gate mechanics the FMRB theme centers on. Flat at 2 tightly-worded rows from 6/14 — still below the bar, wording unchanged. Log-only watch (see recommendation 3); if a third or fourth tightly-worded lab-governance row appears, prefer widening the FMRB description over adding a theme.
- Loader-verification default cluster — 4 rows (training-to-tenant signed default, unified loader-verification chain, GGUF signed-card upload gate + SSTI scan, sandboxed-skill verification harness). Covered by `tech.model_supply_chain` (its description was widened to include loader-verification keywords on 5/24). Stale candidates.
- Local / edge-LLM recipe cluster — 4 rows (27B Dense + 1M context default local recipe, Bonsai 8B 1-bit native training default, local-LLM workstation three-tier signed-load, local-first coding agents cloud-overflow default). Split across `tech.one_bit_edge_llm`, `tech.local_inference_runtime`, and `business.cloud_vs_local_distribution`.
- Physical-AI industrial cluster — 2 rows (physical-AI league tables top-five OEM humanoids, robot-control training as per-robot cloud SaaS). Covered by `tech.physical_ai_robotics`.
- Hyperscaler / compute-coupling cluster — 3 rows (frontier-AI cloud three-cloud training oligopoly, hyperscaler-AI-lab capital coupling lock-in, multi-cloud frontier-model SLA standardization). Covered by `business.hyperscaler_frontier_lab_alliance` and `business.compute_capex_strategy`.
- Open-weight / coding-model benchmark cluster — 5 rows (Kimi K2.7-Code SWE-bench Verified, MiniMax M3 SWE-Bench Pro rerun, US open-weights >54 Artificial Analysis Index, agent-registry-MCP convergence, governed agent-ontology moat). Covered by `business.open_weight_vs_proprietary`, `tech.agent_registry_architecture`, and `tech.agent_control_plane`.
- Sub-threshold scattered rows (≤ 1 each): AI-accelerator equity re-rate on next ASIC print, capital-markets reprice agent-displaced SaaS, AI-assisted vuln triage federal baseline, DOJ Apple-Siri Gemini search-remedies, cross-orchestrator interop matrix, US export-control defensive-cyber carve-out, Corsair-class inference silicon hyperscaler deployment, OpenSharing cross-org asset protocol, Google DeepMind senior-researcher retention package, Unsloth agent-loader mitigation default.

**No new-theme proposal warranted this week.** Every cluster at or above the 3-row bar is already covered by an existing theme. The only emerging cluster worth tracking is frontier-lab voluntary governance/safety-framework convergence, and at 2 rows it is below the bar and better handled (if it grows) by widening the existing FMRB theme than by adding a theme.

## Category-level notes

Category dominance (≥ 50 %): one — `business.capital-supply-chain` at **71.4 %** (back up from 70.2 % on 6/14; the climb 52.2 % → 68.9 % → 74.0 % → 76.0 % resumed after the single-week 70.2 % dip). Still the only category over threshold and still the highest in the project. `tech.security` 41.2 % (the top tech category, below threshold). `tech.infrastructure` 27.1 %. `tech.standards` 14.7 %. `business.regulation-compliance` 14.3 %. The business-side imbalance re-tightened this week: the capex-strategy theme grew but so did the revenue-disclosure theme, so the category re-concentrated rather than balanced, and there is still no theme-level escape valve — every business theme except the two capital-supply-chain themes sits in a different, small-population category. The durable structural fix is a new category, which per §2.3 requires its own design discussion (recommendation 3).

## Recommended actions

### Action 1: Sharpen `business.ai_revenue_disclosure` description (re-emitted — still never landed after 4 weeks)

This is the same vetted sharpening proposed on 5/31, 6/07, and 6/14, re-issued because the schema edit has never been applied (latest schema commit is still `e5e0a79`, applied 5/26; the `Big-3` and `foundation labs (OpenAI` anchors are confirmed present in the current `app/src/schema.sql` by direct grep). The theme over-fits on broad anchor-name tokens ("frontier", "hyperscaler", "AI-accelerator", "AI-services", "Big-3", "audited", bare company names) that let it win the IDF tournament on the off-topic share of its 102 primary kids that are not about AI-revenue disclosure (KV-cache deployment knobs, MoE LoRA recipes, MCP RFCs, Linux-kernel CVE chains, agentic-AI deployment guides, OCC Model Risk Management, sandboxed-skill verification, edge-LLM recipes). The rewrite tightens the keyword set to disclosure mechanics only and explicitly drops the bare company-name and bare-category anchors that pull unrelated predictions in. Expected effect: off-topic primary kids re-route to other themes (or to `theme_candidates` if no other theme matches well), restoring the primary core toward the ~42-prediction genuine-disclosure cluster and locking in the relief of `business.capital-supply-chain` dominance so it does not keep oscillating week-to-week (90 → 124 → 90 → 102 over the last four weeks is exactly the matcher noise the sharpening removes). This is a sharpening, not a widening — total token count goes down by intent. Low-risk: it narrows one over-broad theme on a description swap; predictions that fall out are diagnostic signal for the §2.3 category carve-out conversation.

```action
{
  "kind": "rewrite-description",
  "theme_id": "business.ai_revenue_disclosure",
  "new_description_en": "The 2026 rewrite of how AI revenue is reported to capital markets, focused on disclosure-mechanic vocabulary: SEC AI-revenue concept release; Corporation Finance staff guidance and Staff Legal Bulletins on AI-accelerator IPO disclosure; audited monthly-revenue and WAU disclosure cadence as a recurring reporting primitive; per-token-margin disclosure as a recurring reporting primitive; 10-Q segment footnote breakouts as a disclosure mechanic (AI-services run-rate, AI-accelerator segment footnote, AI-business KPI footnote); AI-accelerator IPO and S-1 disclosure cohort mechanics; per-counterparty risk-factor tables in S-1 filings; hyperscaler-anchor warrant-equity disclosure language; AGI-clause / capability-attestation disclosure language in lab-hyperscaler contracts; AI-accelerator vendor forward-supply 8-K cadence as disclosure primitive; Cerebras / Tenstorrent / Anthropic IPO pricing and post-IPO disclosure mechanics specifically as filings. (Macro and capital-markets regime dynamics around these disclosures are covered by tech.ai_macro_capital_markets, not here. Compute-capex commitments are covered by business.compute_capex_strategy. Hyperscaler-lab partnership structure is covered by business.hyperscaler_frontier_lab_alliance.)",
  "new_description_ja": "2026年のAI売上の資本市場開示再構築、開示メカニクスの語彙に焦点: SEC AI-revenue concept release; AI-accelerator IPO 開示に関する Corporation Finance スタッフガイダンスおよび Staff Legal Bulletins; 反復報告プリミティブとしての監査済み月次売上 + WAU 開示ケイデンス; 反復報告プリミティブとしての per-token margin 開示; 開示メカニクスとしての 10-Q セグメント脚注ブレイクアウト (AI-services ランレート、AI-accelerator セグメント脚注、AI-business KPI 脚注); AI-accelerator IPO/S-1 開示コホートメカニクス; S-1 提出書類のカウンターパーティ別リスク要因表; ハイパースケーラー・アンカー新株予約権-持分開示文言; ラボ-ハイパースケーラー契約の AGI 条項 / 能力アテステーション開示文言; 開示プリミティブとしての AI-accelerator ベンダー先渡し供給 8-K ケイデンス; Cerebras / Tenstorrent / Anthropic の IPO 値付けおよび IPO 後開示メカニクス (具体的に提出書類として)。(これらの開示を取り巻くマクロおよび資本市場レジームのダイナミクスは tech.ai_macro_capital_markets 側でカバー。計算 capex コミットメントは business.compute_capex_strategy 側。ハイパースケーラー-ラボ パートナーシップ構造は business.hyperscaler_frontier_lab_alliance 側。)",
  "new_description_es": "La reescritura de 2026 sobre cómo se reporta el ingreso de IA a los mercados de capital, enfocada en vocabulario de mecánicas de divulgación: SEC AI-revenue concept release; staff guidance de Corporation Finance y Staff Legal Bulletins sobre la divulgación en IPOs de AI-accelerator; cadencia de divulgación de ingresos auditados mensuales + WAU como primitiva de reporte recurrente; divulgación de per-token margin como primitiva de reporte recurrente; breakouts de notas a pie en segmentos 10-Q como mecánica de divulgación (run-rate de AI-services, nota a pie de segmento de AI-accelerator, nota a pie de KPI de AI-business); mecánicas de la cohorte de divulgación de IPO y S-1 de AI-accelerator; tablas de factores de riesgo por contraparte en presentaciones S-1; lenguaje de divulgación de warrants-equity de anclaje de hyperscaler; lenguaje de divulgación de cláusulas AGI / atestación de capacidad en contratos lab-hyperscaler; cadencia de oferta forward 8-K de proveedores de AI-accelerator como primitiva de divulgación; mecánicas de pricing de IPO y divulgación post-IPO específicamente como presentaciones de Cerebras / Tenstorrent / Anthropic. (Las dinámicas de régimen macro y de mercados de capital alrededor de estas divulgaciones las cubre tech.ai_macro_capital_markets, no este tema. Los compromisos de capex de cómputo los cubre business.compute_capex_strategy. La estructura de partnerships hyperscaler-lab la cubre business.hyperscaler_frontier_lab_alliance.)",
  "new_description_fil": "Ang 2026 rewrite kung paano iniuulat ang kita ng AI sa capital markets, nakatuon sa vocabulary ng disclosure mechanics: SEC AI-revenue concept release; staff guidance ng Corporation Finance at mga Staff Legal Bulletin tungkol sa AI-accelerator IPO disclosure; cadence ng audited monthly-revenue at WAU disclosure bilang recurring reporting primitive; per-token-margin disclosure bilang recurring reporting primitive; 10-Q segment footnote breakouts bilang disclosure mechanic (run-rate ng AI-services, AI-accelerator segment footnote, AI-business KPI footnote); mga mechanic ng AI-accelerator IPO at S-1 disclosure cohort; mga per-counterparty risk-factor table sa S-1 filings; hyperscaler-anchor warrant-equity disclosure language; AGI-clause / capability-attestation disclosure language sa mga lab-hyperscaler contract; AI-accelerator vendor forward-supply 8-K cadence bilang disclosure primitive; mechanic ng IPO pricing at post-IPO disclosure ng Cerebras / Tenstorrent / Anthropic partikular bilang mga filing. (Ang macro at capital-markets regime dynamics sa paligid ng mga disclosure na ito ay sakop ng tech.ai_macro_capital_markets, hindi nito. Ang compute-capex commitments ay sakop ng business.compute_capex_strategy. Ang hyperscaler-lab partnership structure ay sakop ng business.hyperscaler_frontier_lab_alliance.)"
}
```

### Action 2: Observation (no schema edit) — two thin-primary themes remain on watch; capex-strategy stays healthy

The 6/14 underused watch posture is unchanged. `business.compute_capex_strategy` is not a concern — it grew again this week (67 / 23 → 82 / 28) and stays a healthy, well-populated theme. Two themes remain on the thin-primary watch list, neither a merge candidate (both carve distinct keyword sub-spaces, both stay visible via healthy secondary-attach child counts): `business.cloud_vs_local_distribution` (26 children, 1 primary — second consecutive week at 1) and `business.ai_security_compliance_market` (24 children, 2 primary). No schema edit this week; re-measure both next Sunday. `business.cloud_vs_local_distribution` is now the stronger keyword-expansion candidate of the two — flag it for a description widening if primary stays ≤ 1 a third consecutive week; flag `business.ai_security_compliance_market` only if primary stays ≤ 2.

```action
{"kind": "log-only"}
```

### Action 3: Observation (no schema edit) — business-scope category carve-out still overdue; frontier-lab-governance cluster flat at 2 rows

Two log-only items folded together. First: `business.capital-supply-chain` rose back to **71.4 %** primary density this week (from 70.2 %), erasing the single-week dip and re-confirming the multi-week climb. It is the only category over the 50 % flag line and the highest single-category dominance in the project. Last week's hope that the capex-strategy revival would keep the category balanced did not hold — both capital-supply-chain themes grew, so the category re-concentrated. The durable fix is a new `business.capital-markets` (or equivalent) category that pulls `business.ai_revenue_disclosure` out of `business.capital-supply-chain`. Per §2.3 a new category requires its own design discussion and is out of scope for the Sunday `apply-schema-edit` flow. Flagged six weeks running; should be promoted to milestone-priority and stop being deferred. Second: the frontier-lab voluntary-governance / safety-framework-convergence candidate cluster stayed flat at 2 tightly-worded Q2-2027 rows (shared quantitative-harm thresholds, catastrophic-risk disclosure alignment) — still below the 3-hit promotion bar and conceptually distinct from the regulatory-board / procurement-gate mechanics `tech.frontier_model_regulatory_board` centers on. If it grows to 3-4 tightly-worded rows next week, prefer a description widening of the existing FMRB theme over a new theme. No action this week.

```action
{"kind": "log-only"}
```

### Action 4: Investigation (no schema edit) — `export.py` secondary-attach threshold + `theme_candidates` label normalisation

Tenth consecutive review flagging the multi-attach pattern; ninth flagging candidate-label uniqueness. Tech multi-attach 64.7 %, business 74.7 %, mix 89.8 % (all up slightly as the pool grew). On the candidate side: 42 pending rows (up from 38), all at within-scope hit count 1; the recurring cluster groups (AI-Revenue/SEC ~9 rows, FMRB 4, Loader-Verification 4, local/edge-LLM 4) are largely stale candidates created the same day their target theme's schema edit landed, plus fresh single-row predictions from this past week. The candidate-extraction pass still appears to run against the keyword set in effect at ingest time and not re-evaluate after a later same-day schema edit, so these inflate the pending count without representing genuine new clusters. Both problems are engineering work outside `apply-schema-edit` scope — `app/src/export.py` secondary-attach threshold and the candidate-extraction label-normalisation + post-schema-edit re-evaluation pass. Deferred again.

```action
{"kind": "log-only"}
```

## Why this rotation

The headline this week is that last Sunday's organic relief reversed and the schema edit still has not landed. `business.ai_revenue_disclosure` primary kids went 90 → 102 (child_ids 141 → 160) and `business.capital-supply-chain` dominance ticked back up 70.2 % → 71.4 %, erasing the single-week dip exactly as 6/14 warned was possible — the relief was matcher noise against fresh predictions, not a structural change. The latest schema commit is still `e5e0a79` (applied 5/26), and the `business.ai_revenue_disclosure` sharpening proposed on 5/31, 6/07, and 6/14 has still never landed (the broad `Big-3` and `foundation labs (OpenAI` anchors are confirmed present in the current schema by direct grep). `business.compute_capex_strategy` continued to grow healthily on its own (23 → 28 primary) but did not absorb load off the disclosure theme this time. The one recommended schema action this Sunday re-emits the 5/31/6/07/6/14 sharpening unchanged so the relief is locked into the schema instead of oscillating weekly; everything else — the still-overdue business-side category carve-out, the two thin-primary themes, the multi-attach / candidate-normalisation pipeline work, and the flat frontier-lab-governance cluster — is logged as observation, consistent with the conservative posture for an unattended `--mode auto` apply.

## Deferred for next week

- **`business.ai_revenue_disclosure` post-sharpen state.** If recommendation 1 finally lands, next Sunday should show primary kids drop from 102 toward the 40-50 range, off-topic predictions re-route or land in `theme_candidates`, and `business.capital-supply-chain` density fall from 71.4 % toward 50-55 %. If the primary count barely moves after the sharpening lands, the matcher is more keyword-promiscuous than expected and the proper fix escalates to the category-level intervention.
- **Business-scope category carve-out** (`business.capital-markets` or equivalent). Per §2.3, requires its own design discussion. Flagged six weeks running; promote to milestone-priority and stop deferring.
- **`business.cloud_vs_local_distribution`** (1 primary, second week running) and **`business.ai_security_compliance_market`** (2 primary, 0 in mix). Watch; flag `cloud_vs_local_distribution` for keyword expansion if primary stays ≤ 1 a third week, `ai_security_compliance_market` only if primary stays ≤ 2. Neither is a merge candidate — the sub-spaces differ.
- **Frontier-lab voluntary-governance cluster.** Currently 2 tightly-worded candidate rows, flat from 6/14. If it grows to 3-4 rows, widen the `tech.frontier_model_regulatory_board` description rather than add a theme.
- **Pipeline-level fixes** (export.py secondary-attach threshold, candidate-extraction label normalisation + post-schema-edit re-evaluation). Carry forward — engineering work outside `apply-schema-edit` scope.

---
