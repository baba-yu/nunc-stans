# Dormant pool — week ending 2026-04-27

Mode: bootstrap-continuation (ad-hoc Mon run, with force-dormant rule applied)

This run was triggered manually on Mon 2026-04-27, one day after the 2026-04-26 Sunday bootstrap, because today's matcher fix and `predictions.created_at → ingested_at` rename changed the ingest pipeline state and the user wanted the weekly artefacts re-stamped against the new DB. Today's run is also the first to apply the **force-dormant rule** added to Step 2 case 3 of `4_weekly_memory.md`: predictions whose origin is strictly older than today's last-7-day window enter the dormant pool directly when their `max_rel` over the last 7 days < 4. The previous wording ("do not touch them this run") had a bootstrap dead-end — fresh predictions could age past 7 days but never enter dormant, because the snapshot's only tier is Dormant ≥ 14d (no Lukewarm tier exists).

Aged-out cohort surveyed (origin in 2026-04-19 or 2026-04-20; today's last-7-day window starts 2026-04-21):

| origin | short label | max_rel last 7d | disposition |
|---|---|---|---|
| 2026-04-19 | Indirect prompt injection becomes a top-bin AI CVE class | 5 | Hot — skip dormant |
| 2026-04-19 | Convergence on an "Agent Registry" model | 5 | Hot — skip dormant |
| 2026-04-19 | 1-bit native training becomes the dominant compression edge | 4 | Hot (boundary) — skip dormant |
| 2026-04-20 | Secret leakage through CI / agents becomes the next battleground | 5 | Hot — skip dormant |
| 2026-04-20 | Acceleration of the "local-first, cloud-overflow" inversion | 5 | Hot — skip dormant |
| **2026-04-20** | **"Headless Everything" architecture standardizes** | **2** | **→ dormant** |

The five Hot predictions remain in the active daily-evaluation flow. The dashboard's **window filter** (concept #2 — `docs/assets/app.js:isVisibleAtZoom` compares `detail.prediction_date` against `manifest.latest_report_date - windowDays`) hides them from the 7d view because their origin (4/19 or 4/20) is past the 7-day cutoff; their 30d / 90d windows still expose the full validation history with whatever per-window `status` (Hot / Lukewarm / no_signal) the scoring pipeline computed. They are deliberately **not** stamped `dormant` because they aren't parked — only the pool snapshot below carries that flag, sourced via `app/src/export.py:_load_dormant_set` for the JSON's `node.detail.dormant`. The three concepts (Hot/Lukewarm, window filter, dormant pool) are pillared independently; see `4_weekly_memory.md` "Three orthogonal concepts" section for the rationale. Hot predictions become eligible for snapshot entry on a future weekly run if and when their `max_rel` drops below 4.

Only the Headless prediction qualifies for the snapshot today.

## Tier: Dormant — interval ≥ 14 days

| ID | Prediction (short) | Signals | First seen | Last relevance | Next ping | Days quiet |
|---|---|---|---|---|---|---|
| 20260420-3 | "Headless Everything" architecture standardizes | headless, headless-preferred, browserless, screen-less UI, API-only agent UI, MCP metadata, Agent Registry, Simon Willison, browser-operating agents, Claude in Chrome, GUI-driving agents | 2026-04-20 | 2 (4/27) | 2026-05-11 | 0 |

(One entrant. Hot 4/19-4/20 predictions skipped per case 3 step 2; they live in the active daily flow plus the export-layer 7d gate. First eligible re-evaluation for the Headless dormant entry: 2026-05-11.)
