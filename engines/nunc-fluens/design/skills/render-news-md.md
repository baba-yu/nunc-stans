# Skill: render-news-md

Deterministic Jinja2 renderer that turns the per-date sourcedata JSON files into `report/<locale>/news-YYYYMMDD.md`. **No LLM.** The renderer is the single producer of the rendered news markdown post-Phase-3.